<!-- NEXT-AGENTS-MD-START -->
Before changing a Next.js codebase, read the relevant docs in `node_modules/next/dist/docs/` first.

If installed docs are unavailable, fall back to these official references:
- Next.js 16.2 release notes
- The AI Coding Agents guide

Prioritize these topics when they are relevant to the task:
- App Router file conventions and route boundaries
- `proxy.ts`
- async `params`, `searchParams`, `cookies()`, and `headers()`
- debugging with browser log forwarding, `next-browser`, server function logging, hydration diff, `next dev --inspect`, and `next start --inspect`
<!-- NEXT-AGENTS-MD-END -->
